{
  "name": "open-react-hub",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "publish-registry": "node scripts/publish-registry.mjs",
    "publish-cli": "node scripts/publish-cli.mjs cli",
    "publish-button": "node scripts/publish-package.mjs button"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "fs-extra": "^11.1.0"
  }
}
